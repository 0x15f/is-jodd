# Live Jev benchmark

This run does not establish a consistent speed advantage: the paired interval includes zero.

Started: 2026-09-22T01:50:30.600Z. Node v24.7.0, darwin/arm64.

is-jodd 1.0.0; is-jeven 1.0.0. Both request the alias `jev-latest`; the resolved server model is not available through these package APIs.

44 requests including 4 warmup requests; 15000 ms timeout per request; no retries. Inputs and call order are stored in the accompanying JSON.

| Method | Attempts | Correct | Incorrect | Errors | Timeouts | Mean ms | Median ms | p95 ms |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| is-jodd | 20 | 20 | 0 | 0 | 0 | 157.51 | 152.87 | 189.21 |
| !is-jeven | 20 | 20 | 0 | 0 | 0 | 155.62 | 147.08 | 213.25 |

Latency columns include successful responses; correctness is measured independently against integer parity. There were 0 answer disagreements.

Among 20 pairs where both answers were correct, is-jodd won 9 pairs (0 ties). Mean paired savings (!is-jeven minus is-jodd): -1.90 ms. Exploratory 95% percentile-bootstrap interval: [-18.09, 12.89] ms, from 10000 resamples with a fixed seed.

Pairs run sequentially with alternating first method. Network conditions, service load, caching, the model alias, and correlated timing noise may affect results. A short run and this bootstrap interval are not proof of a general performance claim. Request and output schemas differ between packages, so any difference cannot be attributed solely to JavaScript negation.
