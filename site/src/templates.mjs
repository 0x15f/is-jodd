import { site, categories, articlePath } from './config.mjs';
export const escapeHtml = (value) => String(value).replace(/[&<>"']/g, c => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c]));
const e = escapeHtml;
const json = (value) => JSON.stringify(value).replace(/</g, '\\u003c');

export function layout({title, description, path='/', content, schema=[], noindex=false}) {
  const canonical = `${site.origin}${path}`;
  const social = process.env.SITE_OG === 'true' ? `<meta property="og:image" content="${site.origin}/og.png"><meta name="twitter:image" content="${site.origin}/og.png"><meta property="og:image:width" content="1730"><meta property="og:image:height" content="909"><meta property="og:image:alt" content="Oddly Specific. Is it odd? A small question. An entire website.">` : '';
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${e(title)}</title><meta name="description" content="${e(description)}"><link rel="canonical" href="${canonical}"><meta name="theme-color" content="#f5f3eb"><meta name="robots" content="${noindex?'noindex, follow':'index, follow'}"><meta property="og:type" content="${schema.some(x=>x['@type']==='Article')?'article':'website'}"><meta property="og:site_name" content="${site.name}"><meta property="og:title" content="${e(title)}"><meta property="og:description" content="${e(description)}"><meta property="og:url" content="${canonical}"><meta name="twitter:creator" content="${site.twitterHandle}"><meta name="twitter:site" content="${site.twitterHandle}"><meta name="twitter:card" content="${social?'summary_large_image':'summary'}">${social}<link rel="icon" href="/favicon.ico" sizes="any"><link rel="stylesheet" href="/style.css"><script type="application/ld+json">${json({'@context':'https://schema.org','@graph':schema})}</script><script type="module" src="/app.mjs"></script></head><body><a class="skip-link" href="#main">Skip to content</a><header class="site-header shell"><a href="/" class="wordmark" aria-label="Oddly Specific home"><span class="brand-dot" aria-hidden="true"></span>oddly specific<span class="brand-period">.</span></a><nav aria-label="Main navigation"><a href="/#calculator">Calculator</a><a href="/learn/" ${path.startsWith('/learn/')?'aria-current="true"':''}>Number basics</a><a href="/code/" ${path.startsWith('/code/')?'aria-current="true"':''}>Code</a><a href="/jev/" ${path.startsWith('/jev/')?'aria-current="true"':''}>Jev lab <span aria-hidden="true">↗</span></a></nav></header><main id="main">${content}</main><footer class="site-footer shell"><div><a class="wordmark" href="/">oddly specific.</a><p>A very long domain for a very short answer.</p><span class="tiny domain">${site.domain}</span></div><div class="footer-links"><a href="/articles/">All guides</a><a href="/about/">About & editorial notes</a><a href="/privacy/">Privacy</a><a href="${site.twitter}" rel="me">X / Twitter ↗</a><a href="${site.github}">GitHub ↗</a></div><div class="footer-bottom"><span>Made by <a href="https://github.com/0x15f">Jake Casto</a>. Curious by design.</span><span>Independent. Not affiliated with TypeSafe AI.</span></div></footer></body></html>`;
}

export function calculator() {
 return `<form id="calculator" class="calculator" data-calculator data-verdict="odd" novalidate><div class="calculator-top"><span class="eyebrow">THE ODD / EVEN CALCULATOR</span><span class="live-dot" aria-hidden="true"></span></div><label for="integer">Give us an integer.</label><div class="input-row"><input id="integer" type="text" inputmode="text" value="127" autocomplete="off" spellcheck="false" aria-describedby="integer-hint integer-error"><button class="calculate-button" type="submit" aria-label="Check whether the integer is odd">↗</button></div><p id="integer-hint" class="input-hint">Negative? Huge? Still an integer. Up to 1,000 digits.</p><p id="integer-error" class="form-error" data-error role="alert"></p><div class="verdict-area" aria-live="polite" aria-atomic="true"><span class="result-kicker">THE VERDICT</span><output class="verdict" for="integer" data-result>Odd.</output><p class="equation" data-equation>127 = 2 × (63) + 1</p><p class="explanation" data-explanation>The last digit is 7. One is left over when you make pairs.</p></div><div class="calculator-bottom"><div class="pair-dots" data-dots data-odd="true" aria-hidden="true">${'<i></i>'.repeat(6)}<i class="leftover"></i></div><div class="examples"><span>TRY</span><button type="button" data-example="0">0</button><button type="button" data-example="-17">−17</button><button type="button" data-example="9007199254740993">A big one ↗</button></div></div><noscript><p>Enable JavaScript to use the calculator. Without it, check the last digit: 1, 3, 5, 7, or 9 means odd.</p></noscript></form>`;
}

export function articleCard(article, index=0) {
  return `<a class="article-card" href="${articlePath(article)}" data-article data-search="${e(`${article.title} ${article.description} ${categories[article.category].name}`.toLowerCase())}"><div class="card-top"><span class="tag ${categories[article.category].color}">${e(categories[article.category].name)}</span><span class="card-number">${String(index+1).padStart(2,'0')}</span></div><h3>${e(article.title)}</h3><p>${e(article.description)}</p><div class="card-bottom"><span>${article.minutes} min read</span><span aria-hidden="true">↗</span></div></a>`;
}

export function home(articles) {
 const featured = ['is-zero-odd-or-even','odd-number-javascript','what-is-jev','can-negative-numbers-be-odd','odd-number-bigint','is-jodd-vs-is-jeven'].map(slug=>articles.find(a=>a.slug===slug)).filter(Boolean);
 const content = `<section class="hero shell"><div class="hero-copy"><p class="eyebrow"><span class="tiny-square"></span> A SMALL QUESTION. AN ENTIRE WEBSITE.</p><h1>How do I calculate if an integer is <em>odd?</em></h1><p class="hero-intro">Divide it by two. If there’s one left over, it’s odd. That’s the answer. We made a whole website anyway.</p><a class="text-link" href="/learn/how-to-tell-if-a-number-is-odd/">Give me the explanation <span>↗</span></a><div class="hero-footnote"><span class="asterisk" aria-hidden="true">✳</span><p>For the curious, the coders,<br>and the spectacularly overthinking.</p></div></div>${calculator()}</section><section class="short-answer shell"><span class="eyebrow">THE ONE-LINE ANSWER</span><code>n % 2 <span>!==</span> 0</code><p>In JavaScript, for a validated integer.<br><a href="/code/odd-number-javascript/">Mind the edge cases ↗</a></p></section><section class="library shell"><div class="section-heading"><div><p class="eyebrow">PICK YOUR LEVEL OF CURIOSITY</p><h2>There’s a little<br>more to <em>odd.</em></h2></div><p>The basics are simple. The details are interesting.<br>Choose your rabbit hole.</p></div><div class="category-grid">${Object.entries(categories).map(([key,cat],i)=>`<a href="/${key}/" class="category-card ${cat.color}"><div class="category-number">0${i+1}<span aria-hidden="true">↗</span></div><span class="eyebrow">${cat.label}</span><h3>${cat.name}</h3><p>${cat.description}</p><span class="category-count">${articles.filter(a=>a.category===key).length} guides</span></a>`).join('')}</div></section><section class="featured shell"><div class="section-heading compact"><div><p class="eyebrow">GOOD PLACES TO START</p><h2>Oddly useful reading.</h2></div><a class="text-link" href="/articles/">All ${articles.length} guides ↗</a></div><div class="article-grid">${featured.map(articleCard).join('')}</div></section><section class="jev-banner shell"><div class="jev-mark" aria-hidden="true">?<span>!</span></div><div><p class="eyebrow">THE EXPERIMENT GOT A LITTLE OUT OF HAND</p><h2>Or you could<br><em>ask an AI.</em></h2><p>Meet is-jodd. One network request to answer a question you could solve with a single digit. We measured it. We published the results.</p><a class="dark-button" href="/jev/">Enter the Jev lab <span>↗</span></a></div><div class="benchmark-note"><span class="metric">861<span>B</span></span><p>of published JavaScript.<br>A very small package of very big ideas.</p><small>Runtime size. Full npm archive: 2,870 B.</small><a href="/jev/jev-parity-benchmarks/">Read the actual benchmarks ↗</a></div></section><section class="closing shell"><p class="eyebrow">ONE LAST THING</p><h2>Zero is even.<br><em>You’re welcome.</em></h2><a class="text-link" href="/learn/is-zero-odd-or-even/">Yes, really. Here’s why ↗</a></section>`;
 return layout({title:'How do I calculate if an integer is odd? | Oddly Specific',description:'Check any integer with our free odd or even calculator. Learn the rules, copy code examples, and explore Jev AI experiments with real benchmark results.',content,schema:[{'@type':'WebSite',name:site.name,url:site.origin,description:'An odd or even calculator and a library of number, code, and Jev guides.'},{'@type':'WebPage',name:'How do I calculate if an integer is odd?',url:site.origin+'/'}]});
}

export function listing(articles, category) {
 const cat = category && categories[category];
 const selected = category ? articles.filter(a=>a.category===category) : articles;
 const path = category ? `/${category}/` : '/articles/';
 const title = cat?.name || 'The whole odd library';
 const intro = cat?.description || 'Every guide, every edge case, every unnecessarily thorough experiment. Find the answer you came for, or a question you didn’t know you had.';
 return layout({title:`${title}: odd numbers and Jev guides | ${site.name}`,description:intro,path,content:`<section class="index-header shell"><p class="eyebrow">${cat?.label || 'A LIBRARY FOR A VERY SPECIFIC QUESTION'}</p><h1>${e(title)}<span class="orange-text">.</span></h1><p class="lede">${e(intro)}</p></section><section class="shell guide-library"><div class="library-controls"><nav class="category-tabs" aria-label="Guide categories"><a href="/articles/" ${!category?'aria-current="page"':''}>All guides</a>${Object.entries(categories).map(([key,c])=>`<a href="/${key}/" ${key===category?'aria-current="page"':''}>${c.name}</a>`).join('')}</nav><label class="search-label"><span class="sr-only">Search these guides</span><input type="search" placeholder="Find a guide…" data-search autocomplete="off"></label></div><p class="guide-count" data-count aria-live="polite">${selected.length} guides</p><div class="article-grid">${selected.map(articleCard).join('')}</div><p class="empty-state" data-empty hidden>No guides match that search. Try “negative”, “JavaScript”, or “Jev”.</p></section>`,schema:[{'@type':'CollectionPage',name:title,description:intro,url:site.origin+path},{'@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:site.origin+'/'},{'@type':'ListItem',position:2,name:title,item:site.origin+path}]}]});
}

export function articlePage(article, html, toc, articles) {
 const cat = categories[article.category];
 const related = article.related.map(slug=>articles.find(a=>a.slug===slug)).filter(Boolean).slice(0,3);
 const path = articlePath(article);
 const sources = article.sources.length ? `<section class="sources"><h2>Sources & further reading</h2><ul>${article.sources.map(s=>`<li><a href="${e(s.url)}">${e(s.title)} ↗</a></li>`).join('')}</ul></section>` : '';
 const content = `<div class="shell"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><a href="/${article.category}/">${cat.name}</a><span>/</span><span>Guide</span></nav><header class="article-header"><span class="tag ${cat.color}">${cat.name}</span><h1>${e(article.title)}</h1><p class="lede">${e(article.description)}</p><div class="byline"><span>By <a href="https://github.com/0x15f">Jake Casto</a></span><span>${article.minutes} min read</span><time datetime="${site.date}">September 22, 2026</time></div></header><div class="article-layout"><article class="article-body"><div class="quick-answer"><span class="eyebrow">THE SHORT ANSWER</span><p>${e(article.summary)}</p></div>${html}${sources}<div class="article-calculator-link"><span>Want to check a number?</span><a class="dark-button" href="/#calculator">Try the calculator ↗</a></div></article><aside class="article-aside">${toc.length ? `<nav aria-label="On this page"><span class="eyebrow">ON THIS PAGE</span>${toc.map(h=>`<a href="#${h.id}">${e(h.text)}</a>`).join('')}</nav>` : ''}<div class="aside-note"><span class="asterisk" aria-hidden="true">✳</span><p>Small question.<br>Proper explanation.</p><a href="/about/">About these guides ↗</a></div></aside></div></div><section class="related shell"><p class="eyebrow">KEEP GOING</p><h2>A few related questions.</h2><div class="article-grid">${related.map(articleCard).join('')}</div></section>`;
 return layout({title:`${article.title} | ${site.name}`,description:article.description,path,content,schema:[{'@type':'Article',headline:article.title,description:article.description,datePublished:site.date,dateModified:site.date,author:{'@type':'Person',name:site.author,url:site.twitter,sameAs:['https://github.com/0x15f',site.twitter]},publisher:{'@type':'Organization',name:site.name,url:site.origin},mainEntityOfPage:site.origin+path,articleSection:cat.name},{'@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:site.origin+'/'},{'@type':'ListItem',position:2,name:cat.name,item:site.origin+`/${article.category}/`},{'@type':'ListItem',position:3,name:article.title,item:site.origin+path}]}]});
}

export function infoPage(kind) {
 const pages = {
  about:{title:'A very specific corner of the internet',description:'About Oddly Specific, its calculator, code examples, Jev experiments, and approach to useful, honest guides.',body:`<p>This site starts with a small question: how do I calculate if an integer is odd? The answer is short. The interesting details fill a library.</p><h2>Useful first. Absurd second.</h2><p>The calculator checks decimal integer text in your browser. It never sends your number to an AI service. It supports up to 1,000 digits and uses the last digit to classify parity, so it avoids JavaScript Number rounding. Our code guides explain which inputs each example accepts.</p><h2>The Jev connection</h2><p>Jake Casto built <a href="https://www.npmjs.com/package/is-jodd">is-jodd</a>, an intentionally overengineered companion to is-jeven. <a href="https://github.com/aurelienbobenrieth">Aurélien Bobenrieth</a> suggested the name. It asks TypeSafe AI’s Jev model whether an integer is odd. That package has a <a href="${site.github}/blob/v1.0.0/LICENSE">custom trillion-dollar license</a>; it is a joke project, with real benchmarks. This free calculator does not call or import the package.</p><h2>How to read our guides</h2><p>Guides provide direct answers, worked examples, explicit edge cases, and links to primary documentation where relevant. The Jev articles reflect documentation consulted on September 22, 2026. Model aliases and API behavior can change; check the linked provider documentation before an integration.</p><p>Our <a href="/jev/jev-parity-benchmarks/">benchmark article</a> reports the full result, including a test that did not show a consistent speed advantage. A short experiment is not a universal performance guarantee.</p><h2>Corrections and independence</h2><p>This is an independent site by <a href="https://github.com/0x15f">Jake Casto</a>. Follow <a href="${site.twitter}" rel="me">@0x15f on X / Twitter</a> for projects and experiments. It is not affiliated with, endorsed by, or operated by TypeSafe AI. Found an issue? <a href="${site.github}/issues">Open a correction on GitHub</a> with the page and a reproducible example.</p>`},
  privacy:{title:'Your number stays with you',description:'The Oddly Specific calculator runs locally in your browser. Read how this static site handles inputs and hosting.',body:`<p>The calculator processes the text you enter in your browser. We do not send calculator inputs to Jev, our server, or an analytics service.</p><h2>No account required</h2><p>This site does not ask you to sign in, collect email addresses, set application cookies, or store your calculator history. Guide search also runs locally.</p><h2>Hosting and external links</h2><p>Cloudflare delivers the site and may process ordinary request information, such as IP addresses and browser details, to operate its network. See <a href="https://www.cloudflare.com/privacypolicy/">Cloudflare’s privacy policy</a>. External sites linked from the guides have their own policies.</p><h2>The npm package is different</h2><p>If you separately install and run is-jodd, it sends the supplied integer to TypeSafe AI using your API key. That is different from the calculator on this website. Read the provider’s terms before sending data.</p><p>Questions or corrections can be raised through <a href="${site.github}/issues">the project’s GitHub issues</a>.</p>`},
  '404':{title:'This page is the odd one out',description:'That page could not be found. Return to the calculator or browse the guide library.',body:'<p>We checked twice. This address does not add up.</p><p><a class="dark-button" href="/">Back to the calculator ↗</a></p><p>Or <a href="/articles/">browse all the guides</a>.</p>'},
 };
 const p = pages[kind];
 return layout({title:`${p.title} | ${site.name}`,description:p.description,path:kind==='404'?'/404/':`/${kind}/`,noindex:kind==='404',content:`<section class="info-page shell"><p class="eyebrow">${kind==='404'?'404 · NOT FOUND':'ODDLY SPECIFIC'}</p><h1>${p.title}<span class="orange-text">.</span></h1><div class="article-body">${p.body}</div></section>`});
}
